package com.example.meet.Enum;

public enum RegistrationStatus {
    SUCCESS,
    INVALID_PASSWORD,
    EMAIL_ALREADY_EXISTS,
    INVALID_EMAIL, FAILURE,
    UPDATE_SUCCESS
    // Thêm các trạng thái đăng ký khác tại đây nếu cần
}
